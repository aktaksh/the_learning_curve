#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<int> vec1 = {9,2,4};
    vec1.push_back(3); // Adds another pointer and expands arraty
    sort(vec1.begin(), vec1.end());
    
for (auto i: vec1)  
    cout << i <<endl;
    return 0;
    //v[1] = 50 insert inject etc
    //v.size() length of array.
    //vector oush pop frnot back
    //Erase values find and erase
    //v.pop_back()
    //v.erase(start to end
    // works as pointer and v.erase (2.4) --> this will not work
    //v.empty(0 -->> checks if vector is empty)
    //vector<vector<int>> 
    matrix = { 
    (1,2,3}
    }
    Vector varname(size,value)  eg 3,7 --> out put will be three 777
    

// Error if  its out of size -- Line 1126: Char 9: runtime error: reference binding to address 0x50200000001c with insufficient space for an object of type 'int' (stl_vector.h) 0x50200000001c: note: pointer points here
//  04 00 00 00 00 00 00 00  00 00 00 00 00 00 00 00  00 00 00 00 00 00 00 00  00 00 00 00 00 00 00 00
}

